# Django Backend
Django>=4.2,<5.0
djangorestframework>=3.14.0
django-cors-headers>=4.3.0
firebase-admin>=6.2.0
requests>=2.31.0
python-dateutil>=2.8.0
gunicorn>=21.2.0
celery[redis]>=5.3,<6.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
dj-database-url>=2.1.0
whitenoise>=6.6.0
# Direct dependency: Fernet encryption of Fitbit tokens at rest
# (fitbit_integration/fields.py). Present transitively via firebase-admin
# too, but pinned here so an upstream change cannot remove it.
cryptography>=42.0
